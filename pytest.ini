[pytest]
minversion = 6.0
addopts = -ra -q
asyncio_mode = auto
testpaths = 
    tests
pythonpath = 
    src
filterwarnings =
    ignore:.*datetime\.datetime\.utcnow.*:DeprecationWarning

markers =
    cli: marks tests that run the full CLI smoke suite
